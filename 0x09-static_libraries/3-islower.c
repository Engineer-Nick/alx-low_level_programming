#include "main.h"
/**
 * _islower - checks for lowercase character in alphabet
 * @c: the character to check
 * Return: 1 if character is lowercase, 0 if else
 */

int _islower(int c)
{
		return (c >= 97 && c <= 122);
}
